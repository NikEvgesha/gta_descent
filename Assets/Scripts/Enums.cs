public enum CurrencyType
{
    Cups,
    Gems,
    Real
}

public enum ShopItemType
{
    Color,
}
public enum EventName
{
    home,
    levelEnd,
    buyColor,
    buyLevel,
    buyInShop,
    adsView,
    gameStart,
}